---
spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    generate-ddl: false
  datasource:
    initialization-mode: always
    schema: "classpath:sql/schema.sql"
    data: "classpath:sql/data.sql"
